[package]
name = "echo-provider"
version = "0.1.0"
edition = "2024"
license = "Unlicense"
publish = false

[dependencies]
axle-types = { path = "../../crates/axle-types", default-features = false }
nexus-init = { path = "../nexus-init", default-features = false }

[features]
default = []
