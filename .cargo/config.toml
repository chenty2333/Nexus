[target.x86_64-unknown-none]
# Keep panic strategy target-scoped so host-side tests retain default behavior.
rustflags = [
    "-C",
    "panic=abort",
    "-C",
    "relocation-model=static",
    "-C",
    "no-redzone=yes",
]

[alias]
nexus = ["run", "--locked", "-p", "nexus-xtask", "--"]
