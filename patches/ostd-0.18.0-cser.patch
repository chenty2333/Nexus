diff --git a/src/arch/x86/iommu/interrupt_remapping/mod.rs b/src/arch/x86/iommu/interrupt_remapping/mod.rs
index 85caab2..69299fb 100644
--- a/src/arch/x86/iommu/interrupt_remapping/mod.rs
+++ b/src/arch/x86/iommu/interrupt_remapping/mod.rs
@@ -8,7 +8,10 @@ use spin::Once;
 pub(super) use table::IntRemappingTable;

 use crate::{
-    arch::iommu::registers::{ExtendedCapabilityFlags, IOMMU_REGS},
+    arch::iommu::{
+        invalidate_interrupt_cache,
+        registers::{ExtendedCapabilityFlags, IOMMU_REGS},
+    },
     info, warn,
 };

@@ -22,15 +25,13 @@ impl IrtEntryHandle {
         self.index
     }

-    pub fn enable(&self, vector: u32) {
-        self.table
-            .set_entry(self.index, table::IrtEntry::new_enabled(vector));
+    pub fn enable(&self, vector: u32, level_triggered: bool) {
+        self.table.set_entry(
+            self.index,
+            table::IrtEntry::new_enabled(vector, level_triggered),
+        );

-        IOMMU_REGS
-            .get()
-            .unwrap()
-            .lock()
-            .invalidate_interrupt_cache();
+        invalidate_interrupt_cache();
     }
 }

diff --git a/src/arch/x86/iommu/interrupt_remapping/table.rs b/src/arch/x86/iommu/interrupt_remapping/table.rs
index 2c95d6e..0158f31 100644
--- a/src/arch/x86/iommu/interrupt_remapping/table.rs
+++ b/src/arch/x86/iommu/interrupt_remapping/table.rs
@@ -165,9 +165,11 @@ pub struct IrtEntry(u128);
 impl IrtEntry {
     /// Creates an enabled entry with no validation,
     ///
-    /// DST = 0, IM = 0, DLM = 0, TM = 0, RH = 0, DM = 0, FPD = 1, P = 1
-    pub(super) fn new_enabled(vector: u32) -> Self {
-        Self(0b11 | ((vector as u128) << 16))
+    /// DST = 0, IM = 0, DLM = 0, RH = 0, DM = 0, FPD = 1, P = 1.
+    /// TM is set exactly when `level_triggered` is true.
+    pub(super) fn new_enabled(vector: u32, level_triggered: bool) -> Self {
+        let trigger_mode = (level_triggered as u128) << 4;
+        Self(0b11 | trigger_mode | ((vector as u128) << 16))
     }

     fn as_raw_u64(&self) -> [u64; 2] {
diff --git a/src/arch/x86/iommu/invalidate/descriptor/mod.rs b/src/arch/x86/iommu/invalidate/descriptor/mod.rs
index 3a60ef4..3d93be4 100644
--- a/src/arch/x86/iommu/invalidate/descriptor/mod.rs
+++ b/src/arch/x86/iommu/invalidate/descriptor/mod.rs
@@ -10,6 +10,21 @@ impl InterruptEntryCache {
     }
 }

+/// A queued-invalidation descriptor that invalidates every cached I/O
+/// translation.  OSTD 0.18 currently installs one shared second-stage page
+/// table in domain zero, so the Stage 5A patch deliberately uses global
+/// granularity rather than manufacturing a per-device/domain identity.
+pub struct Iotlb(pub u128);
+
+impl Iotlb {
+    const INVALIDATION_TYPE: u128 = 2;
+    const GLOBAL_GRANULARITY: u128 = 1 << 4;
+
+    pub fn global_invalidation() -> Self {
+        Self(Self::INVALIDATION_TYPE | Self::GLOBAL_GRANULARITY)
+    }
+}
+
 pub struct InvalidationWait(pub u128);

 impl InvalidationWait {
diff --git a/src/arch/x86/iommu/mod.rs b/src/arch/x86/iommu/mod.rs
index 2749c4c..d8652d6 100644
--- a/src/arch/x86/iommu/mod.rs
+++ b/src/arch/x86/iommu/mod.rs
@@ -20,7 +20,29 @@ pub(in crate::arch) use interrupt_remapping::{
     IrtEntryHandle, alloc_irt_entry, has_interrupt_remapping,
 };

-use crate::{io::IoMemAllocatorBuilder, mm::page_table::PageTableError};
+use registers::{DmaInvalidationPoll, DmaInvalidationTicket, IOMMU_REGS};
+
+use crate::{
+    io::IoMemAllocatorBuilder,
+    irq,
+    mm::{Daddr, page_table::PageTableError},
+};
+
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub(crate) struct IotlbInvalidationTicket(DmaInvalidationTicket);
+
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub(crate) enum BeginIotlbInvalidationError {
+    Busy,
+    PteRemovalFailed,
+}
+
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub(crate) enum PollIotlbInvalidation {
+    Pending,
+    Complete,
+    RetainedFailure,
+}

 /// An enumeration representing possible errors related to IOMMU.
 #[derive(Debug)]
@@ -39,3 +61,81 @@ pub(crate) fn init(io_mem_builder: &IoMemAllocatorBuilder) -> Result<(), IommuEr
     interrupt_remapping::init();
     Ok(())
 }
+
+/// Removes one retained DMA owner's PTEs and submits one global IOTLB
+/// invalidation while OSTD still exclusively owns the VT-d registers and
+/// invalidation queue.
+pub(crate) fn begin_unmap_invalidate(
+    daddr: Daddr,
+) -> Result<IotlbInvalidationTicket, BeginIotlbInvalidationError> {
+    // Prevent same-CPU synchronous interrupt-remapping re-entry between slot
+    // reservation and descriptor submission. This guard covers exactly one
+    // PTE removal and never a hardware-completion wait.
+    let _irq_guard = irq::disable_local();
+    let ticket = {
+        let mut regs = IOMMU_REGS.get().unwrap().lock();
+        regs.reap_completed_invalidation();
+        if !regs.invalidation_slot_available() {
+            return Err(BeginIotlbInvalidationError::Busy);
+        }
+        regs.reserve_dma_invalidation()
+    };
+
+    if unmap(daddr).is_err() {
+        IOMMU_REGS
+            .get()
+            .unwrap()
+            .lock()
+            .retain_failed_invalidation(ticket);
+        return Err(BeginIotlbInvalidationError::PteRemovalFailed);
+    }
+
+    IOMMU_REGS
+        .get()
+        .unwrap()
+        .lock()
+        .submit_dma_invalidation(ticket);
+    Ok(IotlbInvalidationTicket(ticket))
+}
+
+pub(crate) fn poll_unmap_invalidate(ticket: IotlbInvalidationTicket) -> PollIotlbInvalidation {
+    let mut regs = IOMMU_REGS.get().unwrap().lock();
+    match regs.poll_dma_invalidation(ticket.0) {
+        DmaInvalidationPoll::Pending => PollIotlbInvalidation::Pending,
+        DmaInvalidationPoll::Complete => PollIotlbInvalidation::Complete,
+        DmaInvalidationPoll::RetainedFailure => PollIotlbInvalidation::RetainedFailure,
+    }
+}
+
+/// Preserves OSTD's synchronous interrupt-remapping contract without waiting
+/// for hardware while holding the LocalIrqDisabled IOMMU register lock.
+pub(super) fn invalidate_interrupt_cache() {
+    let ticket = loop {
+        let ticket = IOMMU_REGS
+            .get()
+            .unwrap()
+            .lock()
+            .begin_interrupt_cache_invalidation();
+        if let Some(ticket) = ticket {
+            break ticket;
+        }
+        core::hint::spin_loop();
+    };
+
+    loop {
+        let poll = IOMMU_REGS
+            .get()
+            .unwrap()
+            .lock()
+            .poll_dma_invalidation(ticket);
+        match poll {
+            DmaInvalidationPoll::Pending => core::hint::spin_loop(),
+            DmaInvalidationPoll::Complete => return,
+            DmaInvalidationPoll::RetainedFailure => loop {
+                // This legacy API has no error return. Fail closed rather than
+                // publishing an interrupt-table update without invalidation.
+                core::hint::spin_loop();
+            },
+        }
+    }
+}
diff --git a/src/arch/x86/iommu/registers/mod.rs b/src/arch/x86/iommu/registers/mod.rs
index bdfc424..1886223 100644
--- a/src/arch/x86/iommu/registers/mod.rs
+++ b/src/arch/x86/iommu/registers/mod.rs
@@ -8,7 +8,10 @@ mod extended_cap;
 mod invalidation;
 mod status;

-use core::ptr::NonNull;
+use core::{
+    ptr::NonNull,
+    sync::atomic::{Ordering, fence},
+};

 use bit_field::BitField;
 pub use capability::{Capability, CapabilitySagaw};
@@ -33,14 +36,14 @@ use crate::{
             fault,
             invalidate::{
                 QUEUE,
-                descriptor::{InterruptEntryCache, InvalidationWait},
+                descriptor::{InterruptEntryCache, InvalidationWait, Iotlb},
             },
         },
         kernel::acpi::dmar::{Dmar, Remapping},
     },
     debug,
-    io::IoMemAllocatorBuilder,
-    mm::{PAGE_SIZE, paddr_to_vaddr},
+    io::{IoMem, IoMemAllocatorBuilder, Sensitive},
+    mm::{CachePolicy, PAGE_SIZE},
     sync::{LocalIrqDisabled, SpinLock},
 };

@@ -67,6 +70,8 @@ impl IommuVersion {
 /// Important registers used by IOMMU.
 #[derive(Debug)]
 pub struct IommuRegisters {
+    // Owns the kernel mapping that backs every register reference below.
+    _io_mem: IoMem<Sensitive>,
     version: VolatileRef<'static, u32, ReadOnly>,
     capability: VolatileRef<'static, u64, ReadOnly>,
     extended_capability: VolatileRef<'static, u64, ReadOnly>,
@@ -78,6 +83,43 @@ pub struct IommuRegisters {
     interrupt_remapping_table_addr: VolatileRef<'static, u64, ReadWrite>,

     invalidate: InvalidationRegisters,
+
+    // Nexus Stage 5A intentionally supports one ownership-carrying DMA
+    // invalidation at a time.  Keeping the generation beside the single VT-d
+    // register/queue owner lets a later synchronous interrupt-cache
+    // invalidation finish the DMA ticket without losing its completion.
+    next_dma_invalidation_generation: u64,
+    completed_dma_invalidation_generation: u64,
+    active_invalidation: Option<ActiveInvalidation>,
+}
+
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+enum InvalidationPath {
+    Reserved,
+    Queued,
+    DmaRegister,
+    InterruptContextRegister,
+    InterruptIotlbRegister,
+    RetainedFailure,
+}
+
+#[derive(Clone, Copy, Debug)]
+struct ActiveInvalidation {
+    generation: u64,
+    path: InvalidationPath,
+}
+
+/// Opaque identity of one submitted global IOTLB invalidation.
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub(super) struct DmaInvalidationTicket {
+    generation: u64,
+}
+
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub(super) enum DmaInvalidationPoll {
+    Pending,
+    Complete,
+    RetainedFailure,
 }

 impl IommuRegisters {
@@ -195,55 +237,183 @@ impl IommuRegisters {
         self.invalidate.completion_status.as_mut_ptr().write(1);
     }

-    // Invalidates Interrupt-remapping cache.
-    pub(super) fn invalidate_interrupt_cache(&mut self) {
-        if !self.read_global_status().contains(GlobalStatus::QIES) {
-            self.global_invalidation();
+    /// Polls the active generation once. A later caller may publish an
+    /// already-finished generation; the original ticket still observes it via
+    /// `completed_dma_invalidation_generation`.
+    pub(super) fn reap_completed_invalidation(&mut self) {
+        let Some(active) = self.active_invalidation else {
             return;
+        };
+        let _ = self.poll_dma_invalidation(DmaInvalidationTicket {
+            generation: active.generation,
+        });
+    }
+
+    pub(super) fn invalidation_slot_available(&self) -> bool {
+        self.active_invalidation.is_none()
+    }
+
+    /// Reserves the single invalidation generation. The caller releases the
+    /// register lock before changing the second-stage page table.
+    pub(super) fn reserve_dma_invalidation(&mut self) -> DmaInvalidationTicket {
+        assert!(self.active_invalidation.is_none());
+
+        self.next_dma_invalidation_generation = self
+            .next_dma_invalidation_generation
+            .checked_add(1)
+            .expect("DMA invalidation generation overflow");
+        let generation = self.next_dma_invalidation_generation;
+
+        self.active_invalidation = Some(ActiveInvalidation {
+            generation,
+            path: InvalidationPath::Reserved,
+        });
+        DmaInvalidationTicket { generation }
+    }
+
+    pub(super) fn retain_failed_invalidation(&mut self, ticket: DmaInvalidationTicket) {
+        assert!(matches!(
+            self.active_invalidation,
+            Some(ActiveInvalidation {
+                generation,
+                path: InvalidationPath::Reserved,
+            }) if generation == ticket.generation
+        ));
+        self.active_invalidation = Some(ActiveInvalidation {
+            generation: ticket.generation,
+            path: InvalidationPath::RetainedFailure,
+        });
+    }
+
+    /// Submits one global IOTLB invalidation after the caller removed all PTEs
+    /// belonging to the retained DMA owner.
+    pub(super) fn submit_dma_invalidation(&mut self, ticket: DmaInvalidationTicket) {
+        assert!(matches!(
+            self.active_invalidation,
+            Some(ActiveInvalidation {
+                generation,
+                path: InvalidationPath::Reserved,
+            }) if generation == ticket.generation
+        ));
+
+        let path = if self.read_global_status().contains(GlobalStatus::QIES) {
+            self.submit_queued_invalidation(Iotlb::global_invalidation().0);
+            InvalidationPath::Queued
+        } else {
+            // IVT=1 and IIRG=01 request global IOTLB invalidation. Completion
+            // is observed by a later bounded poll after this lock is released.
+            fence(Ordering::Release);
+            self.invalidate
+                .iotlb_invalidate
+                .as_mut_ptr()
+                .write(0x9000_0000_0000_0000);
+            InvalidationPath::DmaRegister
+        };
+
+        self.active_invalidation = Some(ActiveInvalidation {
+            generation: ticket.generation,
+            path,
+        });
+    }
+
+    /// Starts interrupt-cache invalidation, or returns `None` when the one
+    /// hardware slot is occupied. The caller owns any synchronous retry loop.
+    pub(super) fn begin_interrupt_cache_invalidation(&mut self) -> Option<DmaInvalidationTicket> {
+        self.reap_completed_invalidation();
+        if !self.invalidation_slot_available() {
+            return None;
         }

-        let mut queue = QUEUE.get().unwrap().lock();
+        let ticket = self.reserve_dma_invalidation();
+        let path = if self.read_global_status().contains(GlobalStatus::QIES) {
+            self.submit_queued_invalidation(InterruptEntryCache::global_invalidation().0);
+            InvalidationPath::Queued
+        } else {
+            // The legacy non-QI fallback is a two-register sequence. Polling
+            // advances it one bounded step at a time outside this lock.
+            self.context_command
+                .as_mut_ptr()
+                .write(0xA000_0000_0000_0000);
+            InvalidationPath::InterruptContextRegister
+        };
+        self.active_invalidation = Some(ActiveInvalidation {
+            generation: ticket.generation,
+            path,
+        });
+        Some(ticket)
+    }

-        // Currently, we don't support asynchronous processing in the queue. Therefore, when we
-        // lock the queue, we know that it is empty and that the Invalidation Completion Status
-        // Register has been cleared.
+    fn submit_queued_invalidation(&mut self, descriptor: u128) {
+        let mut queue = QUEUE.get().unwrap().lock();

-        // Construct an Interrupt Entry Cache Invalidate Descriptor.
-        queue.append_descriptor(InterruptEntryCache::global_invalidation().0);
-        // Construct an Invalidation Wait Descriptor. We need to set the interrupt flag so that the
-        // Invalidation Completion Status Register can report the completion status.
+        // Clear completion from the preceding single-owner use, then publish
+        // the descriptor and its ordered completion witness.
+        self.invalidate.completion_status.as_mut_ptr().write(1);
+        queue.append_descriptor(descriptor);
         queue.append_descriptor(InvalidationWait::with_interrupt_flag().0);

-        // Update the queue tail.
+        // PTE removals and descriptor stores must be visible before hardware
+        // observes the invalidation-queue tail.
+        fence(Ordering::Release);
         let tail = queue.tail();
         self.invalidate
             .queue_tail
             .as_mut_ptr()
             .write((tail << 4) as u64);
-
-        // Wait for completion.
-        while self.invalidate.completion_status.as_ptr().read() == 0 {}
-        // Clear the Invalidation Completion Status Register.
-        self.invalidate.completion_status.as_mut_ptr().write(1);
     }

-    fn global_invalidation(&mut self) {
-        // Set ICC(63) to 1 to requests invalidation and CIRG(62:61) to 01 to indicate global invalidation request.
-        self.context_command
-            .as_mut_ptr()
-            .write(0xA000_0000_0000_0000);
+    /// Performs one bounded completion observation while the caller holds the
+    /// register lock. No branch waits for hardware.
+    pub(super) fn poll_dma_invalidation(
+        &mut self,
+        ticket: DmaInvalidationTicket,
+    ) -> DmaInvalidationPoll {
+        if ticket.generation <= self.completed_dma_invalidation_generation {
+            return DmaInvalidationPoll::Complete;
+        }

-        // Wait for invalidation complete (ICC set to 0).
-        let mut value = 0x8000_0000_0000_0000;
-        while (value & 0x8000_0000_0000_0000) != 0 {
-            value = self.context_command.as_ptr().read();
+        let Some(active) = self.active_invalidation else {
+            return DmaInvalidationPoll::RetainedFailure;
+        };
+        if active.generation != ticket.generation {
+            return DmaInvalidationPoll::RetainedFailure;
         }

-        // Set IVT(63) to 1 to requests IOTLB invalidation and IIRG(61:60) to 01 to indicate global invalidation request.
-        self.invalidate
-            .iotlb_invalidate
-            .as_mut_ptr()
-            .write(0x9000_0000_0000_0000);
+        let complete = match active.path {
+            InvalidationPath::Reserved => false,
+            InvalidationPath::Queued => self.invalidate.completion_status.as_ptr().read() != 0,
+            InvalidationPath::DmaRegister | InvalidationPath::InterruptIotlbRegister => {
+                self.invalidate.iotlb_invalidate.as_ptr().read() & (1 << 63) == 0
+            }
+            InvalidationPath::InterruptContextRegister => {
+                if self.context_command.as_ptr().read() & (1 << 63) != 0 {
+                    false
+                } else {
+                    self.invalidate
+                        .iotlb_invalidate
+                        .as_mut_ptr()
+                        .write(0x9000_0000_0000_0000);
+                    self.active_invalidation = Some(ActiveInvalidation {
+                        generation: active.generation,
+                        path: InvalidationPath::InterruptIotlbRegister,
+                    });
+                    return DmaInvalidationPoll::Pending;
+                }
+            }
+            InvalidationPath::RetainedFailure => {
+                return DmaInvalidationPoll::RetainedFailure;
+            }
+        };
+        if !complete {
+            return DmaInvalidationPoll::Pending;
+        }
+
+        if active.path == InvalidationPath::Queued {
+            self.invalidate.completion_status.as_mut_ptr().write(1);
+        }
+        self.completed_dma_invalidation_generation = active.generation;
+        self.active_invalidation = None;
+        DmaInvalidationPoll::Complete
     }

     /// Writes value to the global command register. This function will not wait until the command
@@ -286,8 +456,11 @@ impl IommuRegisters {
         assert_ne!(base_address, 0, "IOMMU address should not be zero");
         debug!("base address: {:#x?}", base_address);

-        io_mem_builder.remove(base_address as usize..(base_address as usize + PAGE_SIZE));
-        let base = NonNull::new(paddr_to_vaddr(base_address as usize) as *mut u8).unwrap();
+        let io_mem = io_mem_builder.reserve(
+            base_address as usize..(base_address as usize + PAGE_SIZE),
+            CachePolicy::Uncacheable,
+        );
+        let base = NonNull::new(io_mem.base() as *mut u8).unwrap();

         // SAFETY:
         // - We trust the ACPI tables (as well as the DRHD in them), from which the base address is
@@ -298,6 +471,7 @@ impl IommuRegisters {
             fault::init(base);

             Self {
+                _io_mem: io_mem,
                 version: VolatileRef::new_read_only(base.cast::<u32>()),
                 capability: VolatileRef::new_read_only(base.add(0x08).cast::<u64>()),
                 extended_capability: VolatileRef::new_read_only(base.add(0x10).cast::<u64>()),
@@ -312,6 +486,9 @@ impl IommuRegisters {
                 interrupt_remapping_table_addr: VolatileRef::new(base.add(0xb8).cast::<u64>()),

                 invalidate: InvalidationRegisters::new(base),
+                next_dma_invalidation_generation: 0,
+                completed_dma_invalidation_generation: 0,
+                active_invalidation: None,
             }
         };

diff --git a/src/arch/x86/irq/chip/ioapic.rs b/src/arch/x86/irq/chip/ioapic.rs
index d105f93..ff70d6c 100644
--- a/src/arch/x86/irq/chip/ioapic.rs
+++ b/src/arch/x86/irq/chip/ioapic.rs
@@ -8,6 +8,23 @@ use crate::{
     irq::IrqLine,
 };

+use super::{GsiConfig, GsiPolarity, GsiTriggerMode};
+
+const RTE_ACTIVE_LOW: u32 = 1 << 13;
+const RTE_LEVEL_TRIGGERED: u32 = 1 << 15;
+
+const fn rte_config_bits(config: GsiConfig) -> u32 {
+    let polarity = match config.polarity() {
+        GsiPolarity::ActiveHigh => 0,
+        GsiPolarity::ActiveLow => RTE_ACTIVE_LOW,
+    };
+    let trigger = match config.trigger_mode() {
+        GsiTriggerMode::Edge => 0,
+        GsiTriggerMode::Level => RTE_LEVEL_TRIGGERED,
+    };
+    polarity | trigger
+}
+
 /// I/O Advanced Programmable Interrupt Controller (APIC).
 ///
 /// It is used to distribute external interrupts in a more advanced manner than that of the
@@ -66,7 +83,7 @@ impl IoApic {
     ///
     /// This method will fail if the index exceeds the I/O APIC's maximum redirection entry, or if
     /// the entry is in use.
-    pub(super) fn enable(&mut self, index: u8, irq: &IrqLine) -> Result<()> {
+    pub(super) fn enable(&mut self, index: u8, irq: &IrqLine, config: GsiConfig) -> Result<()> {
         if index > self.max_redirection_entry {
             return Err(Error::InvalidArgs);
         }
@@ -81,7 +98,8 @@ impl IoApic {
             // Intel(R) Virtualization Technology for Directed I/O (Revision 5.0), Section 5.1.5.1
             // I/OxAPIC Programming says "Bit 48 in the I/OxAPIC RTE is Set to indicate the
             // Interrupt is in Remappable format."
-            let mut value: u64 = irq.num() as u64 | 0x1_0000_0000_0000;
+            let mut value: u64 =
+                irq.num() as u64 | u64::from(rte_config_bits(config)) | 0x1_0000_0000_0000;

             // "The Interrupt_Index[14:0] is programmed in bits 63:49 of the I/OxAPIC RTE. The most
             // significant bit of the Interrupt_Index (Interrupt_Index[15]) is programmed in bit 11
@@ -105,8 +123,10 @@ impl IoApic {
             // SAFETY: `index` is inbound. It is safe to enable the redirection entry with the
             // legal IRQ number.
             unsafe {
-                self.access
-                    .write(IoApicAccess::IOREDTBL + 2 * index, irq.num() as u32);
+                self.access.write(
+                    IoApicAccess::IOREDTBL + 2 * index,
+                    irq.num() as u32 | rte_config_bits(config),
+                );
                 self.access.write(IoApicAccess::IOREDTBL + 2 * index + 1, 0);
             }
         }
diff --git a/src/arch/x86/irq/chip/mod.rs b/src/arch/x86/irq/chip/mod.rs
index bda5ce2..aa5fc2b 100644
--- a/src/arch/x86/irq/chip/mod.rs
+++ b/src/arch/x86/irq/chip/mod.rs
@@ -17,6 +17,65 @@ use crate::{
 mod ioapic;
 mod pic;

+/// Electrical polarity of a Global System Interrupt input.
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub enum GsiPolarity {
+    /// The input is asserted at a high voltage level.
+    ActiveHigh,
+    /// The input is asserted at a low voltage level.
+    ActiveLow,
+}
+
+/// Trigger mode of a Global System Interrupt input.
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub enum GsiTriggerMode {
+    /// An inactive-to-active transition asserts the interrupt.
+    Edge,
+    /// The active electrical level asserts the interrupt.
+    Level,
+}
+
+/// Routing configuration shared by the I/O APIC RTE and interrupt-remapping
+/// entry for one GSI mapping.
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub struct GsiConfig {
+    polarity: GsiPolarity,
+    trigger_mode: GsiTriggerMode,
+}
+
+impl GsiConfig {
+    /// The historical OSTD routing behavior.
+    pub const EDGE_HIGH: Self = Self::new(GsiPolarity::ActiveHigh, GsiTriggerMode::Edge);
+
+    /// Creates an explicit GSI routing configuration.
+    pub const fn new(polarity: GsiPolarity, trigger_mode: GsiTriggerMode) -> Self {
+        Self {
+            polarity,
+            trigger_mode,
+        }
+    }
+
+    /// Returns the configured electrical polarity.
+    pub const fn polarity(self) -> GsiPolarity {
+        self.polarity
+    }
+
+    /// Returns the configured trigger mode.
+    pub const fn trigger_mode(self) -> GsiTriggerMode {
+        self.trigger_mode
+    }
+
+    const fn is_level_triggered(self) -> bool {
+        matches!(self.trigger_mode, GsiTriggerMode::Level)
+    }
+}
+
+impl Default for GsiConfig {
+    fn default() -> Self {
+        Self::EDGE_HIGH
+    }
+}
+
 /// An IRQ chip.
 ///
 /// This abstracts the hardware IRQ chips (or IRQ controllers), allowing the bus or device drivers
@@ -50,6 +109,19 @@ impl IrqChip {
         &'static self,
         irq_line: IrqLine,
         gsi_index: u32,
+    ) -> Result<MappedIrqLine> {
+        self.map_gsi_pin_to_with_config(irq_line, gsi_index, GsiConfig::EDGE_HIGH)
+    }
+
+    /// Maps a GSI pin with explicit electrical polarity and trigger mode.
+    ///
+    /// The trigger mode is installed in both the I/O APIC redirection entry
+    /// and, when interrupt remapping is active, the corresponding IRTE.
+    pub fn map_gsi_pin_to_with_config(
+        &'static self,
+        irq_line: IrqLine,
+        gsi_index: u32,
+        config: GsiConfig,
     ) -> Result<MappedIrqLine> {
         let mut io_apics = self.io_apics.lock();

@@ -61,11 +133,16 @@ impl IrqChip {
         let index_in_io_apic = (gsi_index - io_apic.interrupt_base())
             .try_into()
             .map_err(|_| Error::InvalidArgs)?;
-        io_apic.enable(index_in_io_apic, &irq_line)?;
+        irq_line.claim_remapping_trigger_mode(config.is_level_triggered())?;
+        if let Err(error) = io_apic.enable(index_in_io_apic, &irq_line, config) {
+            irq_line.release_remapping_trigger_mode();
+            return Err(error);
+        }

         Ok(MappedIrqLine {
             irq_line,
             gsi_index,
+            config,
             irq_chip: self,
         })
     }
@@ -125,6 +202,7 @@ impl IrqChip {
 pub struct MappedIrqLine {
     irq_line: IrqLine,
     gsi_index: u32,
+    config: GsiConfig,
     irq_chip: &'static IrqChip,
 }

@@ -133,6 +211,7 @@ impl fmt::Debug for MappedIrqLine {
         f.debug_struct("MappedIrqLine")
             .field("irq_line", &self.irq_line)
             .field("gsi_index", &self.gsi_index)
+            .field("config", &self.config)
             .finish_non_exhaustive()
     }
 }
@@ -153,7 +232,8 @@ impl DerefMut for MappedIrqLine {

 impl Drop for MappedIrqLine {
     fn drop(&mut self) {
-        self.irq_chip.disable_gsi(self.gsi_index)
+        self.irq_chip.disable_gsi(self.gsi_index);
+        self.irq_line.release_remapping_trigger_mode();
     }
 }

diff --git a/src/arch/x86/irq/mod.rs b/src/arch/x86/irq/mod.rs
index 6e5b8e3..58dd660 100644
--- a/src/arch/x86/irq/mod.rs
+++ b/src/arch/x86/irq/mod.rs
@@ -14,7 +14,7 @@ pub(super) mod ipi;
 mod ops;
 mod remapping;

-pub use chip::{IRQ_CHIP, IrqChip, MappedIrqLine};
+pub use chip::{GsiConfig, GsiPolarity, GsiTriggerMode, IRQ_CHIP, IrqChip, MappedIrqLine};
 pub(crate) use ipi::{HwCpuId, send_ipi};
 pub(crate) use ops::{
     disable_local, disable_local_and_halt, enable_local, enable_local_and_halt, is_local_enabled,
diff --git a/src/arch/x86/irq/remapping.rs b/src/arch/x86/irq/remapping.rs
index 03bf9f9..fc8bfcb 100644
--- a/src/arch/x86/irq/remapping.rs
+++ b/src/arch/x86/irq/remapping.rs
@@ -25,11 +25,19 @@ impl IrqRemapping {
         self.entry.call_once(|| {
             // Allocate and enable the IRT entry.
             let handle = alloc_irt_entry().unwrap();
-            handle.enable(irq_num as u32);
+            handle.enable(irq_num as u32, false);
             handle
         });
     }

+    /// Keeps the IRTE trigger mode synchronized with an I/O APIC mapping.
+    pub(crate) fn set_trigger_mode(&self, irq_num: u8, level_triggered: bool) {
+        let Some(handle) = self.entry.get() else {
+            return;
+        };
+        handle.enable(irq_num as u32, level_triggered);
+    }
+
     /// Gets the remapping index of the IRQ line.
     ///
     /// This method will return `None` if interrupt remapping is disabled or
diff --git a/src/io/io_mem/mod.rs b/src/io/io_mem/mod.rs
index 5aeb557..3f200e8 100644
--- a/src/io/io_mem/mod.rs
+++ b/src/io/io_mem/mod.rs
@@ -8,6 +8,7 @@ pub(crate) mod util;
 use core::{
     marker::PhantomData,
     ops::{Deref, Range},
+    ptr::NonNull,
 };

 use align_ext::AlignExt;
@@ -156,7 +157,7 @@ impl<SecuritySensitivity> IoMem<SecuritySensitivity> {
     }

     /// Returns the base virtual address of the MMIO range.
-    fn base(&self) -> usize {
+    pub(crate) fn base(&self) -> usize {
         self.kvirt_area.deref().start() + self.offset
     }

@@ -211,6 +212,24 @@ impl IoMem<Sensitive> {
 }

 impl IoMem<Insensitive> {
+    /// Returns a non-null raw pointer to the start of this MMIO range.
+    ///
+    /// This narrow escape hatch exists for device transports that require a
+    /// raw MMIO base. The pointer is owner-bound: it does not keep the mapping
+    /// alive and is valid for at most this `IoMem` owner's range.
+    ///
+    /// # Safety
+    ///
+    /// The caller must retain this `IoMem` owner for every use of the returned
+    /// pointer, keep all accesses within this owner's range, and use the MMIO
+    /// access semantics required by the device. The caller must also prevent
+    /// overlapping alias access through this pointer, this `IoMem`, or any of
+    /// its clones or slices.
+    pub unsafe fn as_non_null_ptr(&self) -> NonNull<u8> {
+        NonNull::new(self.base() as *mut u8)
+            .expect("IoMem mappings never use the null virtual address")
+    }
+
     /// Acquires an `IoMem` instance for the given range.
     ///
     /// The I/O memory cache policy is set to uncacheable by default.
diff --git a/src/irq/top_half.rs b/src/irq/top_half.rs
index ae31a2b..ec07bde 100644
--- a/src/irq/top_half.rs
+++ b/src/irq/top_half.rs
@@ -107,6 +107,34 @@ impl IrqLine {
     pub fn remapping_index(&self) -> Option<u16> {
         self.inner.remapping.remapping_index()
     }
+
+    #[cfg(target_arch = "x86_64")]
+    pub(crate) fn claim_remapping_trigger_mode(&self, level_triggered: bool) -> Result<()> {
+        let mut claim = self.inner.remapping_claim.lock();
+        if claim.users != 0 && claim.level_triggered != level_triggered {
+            return Err(Error::InvalidArgs);
+        }
+        if claim.users == 0 {
+            self.inner
+                .remapping
+                .set_trigger_mode(self.num(), level_triggered);
+            claim.level_triggered = level_triggered;
+        }
+        claim.users = claim
+            .users
+            .checked_add(1)
+            .expect("IRQ remapping trigger-mode claimant overflow");
+        Ok(())
+    }
+
+    #[cfg(target_arch = "x86_64")]
+    pub(crate) fn release_remapping_trigger_mode(&self) {
+        let mut claim = self.inner.remapping_claim.lock();
+        claim.users = claim
+            .users
+            .checked_sub(1)
+            .expect("IRQ remapping trigger mode released without a claim");
+    }
 }

 impl Clone for IrqLine {
@@ -121,6 +149,14 @@ impl Clone for IrqLine {
 struct Inner {
     callbacks: RwLock<Vec<Box<IrqCallbackFunction>>, WriteIrqDisabled>,
     remapping: IrqRemapping,
+    #[cfg(target_arch = "x86_64")]
+    remapping_claim: SpinLock<RemappingClaim>,
+}
+
+#[cfg(target_arch = "x86_64")]
+struct RemappingClaim {
+    users: usize,
+    level_triggered: bool,
 }

 impl Inner {
@@ -128,6 +164,11 @@ impl Inner {
         Self {
             callbacks: RwLock::new(Vec::new()),
             remapping: IrqRemapping::new(),
+            #[cfg(target_arch = "x86_64")]
+            remapping_claim: SpinLock::new(RemappingClaim {
+                users: 0,
+                level_triggered: false,
+            }),
         }
     }
 }
diff --git a/src/mm/dma/dma_coherent.rs b/src/mm/dma/dma_coherent.rs
index d0ecf8d..93a806d 100644
--- a/src/mm/dma/dma_coherent.rs
+++ b/src/mm/dma/dma_coherent.rs
@@ -1,9 +1,11 @@
 // SPDX-License-Identifier: MPL-2.0

-use core::{fmt::Debug, mem::ManuallyDrop};
+use core::{fmt::Debug, mem::ManuallyDrop, ptr::NonNull};

 use super::util::{
-    alloc_kva, cvm_need_private_protection, prepare_dma, split_daddr, unprepare_dma,
+    BeginDmaUnmap, PendingDmaUnmap as IotlbPendingDmaUnmap, PollDmaUnmap, alloc_kva,
+    begin_dma_unmap, complete_unprepare_dma, cvm_need_private_protection, poll_dma_unmap,
+    prepare_dma, split_daddr, unprepare_dma,
 };
 use crate::{
     arch::irq,
@@ -14,6 +16,7 @@ use crate::{
         io::util::{HasVmReaderWriter, VmReaderWriterIdentity},
         kspace::kvirt_area::KVirtArea,
     },
+    warn,
 };

 /// A DMA memory object that can be accessed in a cache-coherent manner.
@@ -30,6 +33,47 @@ pub struct DmaCoherent {
     inner: Inner,
     map_daddr: Option<Daddr>,
     is_cache_coherent: bool,
+    prepared: bool,
+}
+
+/// Why a retained DMA owner cannot currently make progress.  The Stage 5A
+/// patch keeps the backing pages and IOVA owned in either case.
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub enum DmaUnmapFailure {
+    /// Some PTE or the single invalidation engine entered an uncertain state;
+    /// all DMA ownership is retained.
+    PteRemovalOrInvalidationEngine,
+}
+
+#[derive(Clone, Copy, Debug)]
+enum PendingState {
+    Direct,
+    WaitingForInvalidationEngine,
+    Invalidating(IotlbPendingDmaUnmap),
+    Retained(DmaUnmapFailure),
+}
+
+/// An ownership-carrying DMA unmap.  Dropping it intentionally leaks the
+/// retained owner; only a matching IOTLB completion can produce
+/// [`UnmappedDma`].
+#[derive(Debug)]
+pub struct PendingDmaUnmap {
+    dma: ManuallyDrop<DmaCoherent>,
+    paddr: Paddr,
+    size: usize,
+    daddr: Option<Daddr>,
+    state: PendingState,
+    inject_pending_once: bool,
+}
+
+/// A DMA allocation whose device mapping and PADDR tracking are gone.  The
+/// backing allocation is private and is safely released when this value drops.
+#[derive(Debug)]
+pub struct UnmappedDma {
+    _dma: DmaCoherent,
+    original_paddr: Paddr,
+    original_daddr: Option<Daddr>,
+    size: usize,
 }

 #[derive(Debug)]
@@ -39,6 +83,13 @@ enum Inner {
 }

 impl DmaCoherent {
+    /// Receipt-only observation used to prove that Stage 5A begin/poll paths
+    /// pair every local-IRQ guard. This is not part of the ownership protocol.
+    #[doc(hidden)]
+    pub fn stage5a_local_irq_enabled() -> bool {
+        irq::is_local_enabled()
+    }
+
     /// Allocates a region of physical memory for coherent DMA access.
     ///
     /// The memory of the newly-allocated DMA buffer is initialized to zeros.
@@ -88,8 +139,190 @@ impl DmaCoherent {
             inner,
             map_daddr,
             is_cache_coherent,
+            prepared: true,
         })
     }
+
+    /// Returns the base CPU pointer for exclusive raw DMA access.
+    ///
+    /// This narrow escape hatch is intended for audited driver libraries that
+    /// require a stable raw pointer for a retained DMA owner.
+    ///
+    /// # Safety
+    ///
+    /// The caller must retain this `DmaCoherent` owner for the entire pointer
+    /// lifetime, keep every access within `self.size()`, and prevent all
+    /// overlapping access through `VmReader`, `VmWriter`, or another raw
+    /// pointer while the returned pointer is active.
+    ///
+    /// Raw access must stop before DMA teardown or owner destruction begins.
+    pub unsafe fn as_non_null_ptr_exclusive(&self) -> NonNull<u8> {
+        NonNull::new(self.writer().cursor())
+            .expect("DMA mappings never use the null virtual address")
+    }
+
+    /// Begins ownership-safe DMA teardown after the caller has stopped every
+    /// device that can issue transactions through this mapping.
+    ///
+    /// The returned handle owns the backing pages and IOVA until
+    /// [`PendingDmaUnmap::poll_complete`] observes a real IOTLB completion.
+    /// Nexus, rather than OSTD, owns deadlines and tombstone policy.
+    ///
+    /// # Safety
+    ///
+    /// The caller must prove that the device is quiesced and cannot initiate
+    /// new DMA through this object.  IOTLB invalidation only removes cached
+    /// translations; it does not stop a device.
+    pub unsafe fn begin_unmap_invalidate(self) -> PendingDmaUnmap {
+        assert!(self.prepared, "DMA object was already unprepared");
+        assert_eq!(
+            self.size(),
+            PAGE_SIZE,
+            "Nexus Stage 5A exposes ownership-carrying unmap only for one page"
+        );
+        let paddr = self.paddr();
+        let size = self.size();
+        let daddr = self.map_daddr;
+        let state = match begin_dma_unmap(daddr) {
+            BeginDmaUnmap::Direct => PendingState::Direct,
+            BeginDmaUnmap::Busy => PendingState::WaitingForInvalidationEngine,
+            BeginDmaUnmap::Invalidating(pending) => PendingState::Invalidating(pending),
+            BeginDmaUnmap::RetainedFailure => {
+                PendingState::Retained(DmaUnmapFailure::PteRemovalOrInvalidationEngine)
+            }
+        };
+
+        PendingDmaUnmap {
+            dma: ManuallyDrop::new(self),
+            paddr,
+            size,
+            daddr,
+            state,
+            inject_pending_once: false,
+        }
+    }
+}
+
+impl PendingDmaUnmap {
+    /// Test-only fault-injection hook used by the Nexus QEMU receipt.  It does
+    /// not acknowledge hardware and leaves all ownership in this handle.
+    #[doc(hidden)]
+    pub fn inject_one_pending_poll(&mut self) {
+        self.inject_pending_once = true;
+    }
+
+    #[must_use]
+    /// Returns the physical address whose backing owner is retained.
+    pub const fn retained_paddr(&self) -> Paddr {
+        self.paddr
+    }
+
+    #[must_use]
+    /// Returns the retained IOVA, or `None` for a direct mapping.
+    pub const fn retained_daddr(&self) -> Option<Daddr> {
+        self.daddr
+    }
+
+    #[must_use]
+    /// Returns the retained mapping size in bytes.
+    pub const fn retained_size(&self) -> usize {
+        self.size
+    }
+
+    #[must_use]
+    /// Returns a permanent fail-closed reason, if one was observed.
+    pub const fn failure(&self) -> Option<DmaUnmapFailure> {
+        match self.state {
+            PendingState::Retained(reason) => Some(reason),
+            _ => None,
+        }
+    }
+
+    /// Polls once without blocking.  `Err(self)` retains backing ownership and
+    /// may be retried or moved into a Nexus tombstone.
+    pub fn poll_complete(mut self) -> Result<UnmappedDma, Self> {
+        if self.inject_pending_once {
+            self.inject_pending_once = false;
+            return Err(self);
+        }
+
+        match self.state {
+            PendingState::Direct => Ok(self.finish()),
+            PendingState::WaitingForInvalidationEngine => {
+                self.state = match begin_dma_unmap(self.daddr) {
+                    BeginDmaUnmap::Direct => return Ok(self.finish()),
+                    BeginDmaUnmap::Busy => return Err(self),
+                    BeginDmaUnmap::Invalidating(pending) => PendingState::Invalidating(pending),
+                    BeginDmaUnmap::RetainedFailure => {
+                        PendingState::Retained(DmaUnmapFailure::PteRemovalOrInvalidationEngine)
+                    }
+                };
+                Err(self)
+            }
+            PendingState::Invalidating(pending) => match poll_dma_unmap(pending) {
+                PollDmaUnmap::Pending => Err(self),
+                PollDmaUnmap::Complete => Ok(self.finish()),
+                PollDmaUnmap::RetainedFailure => {
+                    self.state =
+                        PendingState::Retained(DmaUnmapFailure::PteRemovalOrInvalidationEngine);
+                    Err(self)
+                }
+            },
+            PendingState::Retained(_) => Err(self),
+        }
+    }
+
+    fn finish(self) -> UnmappedDma {
+        let paddr_range = self.paddr..self.paddr + self.size;
+        let daddr_range = self.daddr.map(|start| start..start + self.size);
+        // SAFETY: Direct mappings require the caller's device-quiescence
+        // promise.  Remapped paths reach here only after the ticket completed.
+        unsafe { complete_unprepare_dma(&paddr_range, daddr_range) };
+
+        let mut this = ManuallyDrop::new(self);
+        // SAFETY: `this` will not run `PendingDmaUnmap::drop`, and the nested
+        // owner is taken exactly once into `UnmappedDma`.
+        let mut dma = unsafe { ManuallyDrop::take(&mut this.dma) };
+        dma.prepared = false;
+        dma.map_daddr = None;
+        UnmappedDma {
+            _dma: dma,
+            original_paddr: this.paddr,
+            original_daddr: this.daddr,
+            size: this.size,
+        }
+    }
+}
+
+impl Drop for PendingDmaUnmap {
+    fn drop(&mut self) {
+        // `dma` is ManuallyDrop: losing the cleanup worker quarantines the
+        // actual frame/IOVA owner instead of calling the unsafe legacy Drop.
+        warn!(
+            "quarantining abandoned DMA unmap owner: paddr={:#x}, daddr={:?}, size={}",
+            self.paddr, self.daddr, self.size
+        );
+    }
+}
+
+impl UnmappedDma {
+    #[must_use]
+    /// Returns the physical address held by the original DMA owner.
+    pub const fn original_paddr(&self) -> Paddr {
+        self.original_paddr
+    }
+
+    #[must_use]
+    /// Returns the original IOVA, or `None` for a direct mapping.
+    pub const fn original_daddr(&self) -> Option<Daddr> {
+        self.original_daddr
+    }
+
+    #[must_use]
+    /// Returns the original mapping size in bytes.
+    pub const fn size(&self) -> usize {
+        self.size
+    }
 }

 impl Split for DmaCoherent {
@@ -97,13 +330,14 @@ impl Split for DmaCoherent {
         assert!(offset.is_multiple_of(PAGE_SIZE));
         assert!(0 < offset && offset < self.size());

-        let (inner, map_daddr, is_cache_coherent) = {
+        let (inner, map_daddr, is_cache_coherent, prepared) = {
             let this = ManuallyDrop::new(self);
             (
                 // SAFETY: `this.inner` will never be used or dropped later.
                 unsafe { core::ptr::read(&this.inner as *const Inner) },
                 this.map_daddr,
                 this.is_cache_coherent,
+                this.prepared,
             )
         };

@@ -126,11 +360,13 @@ impl Split for DmaCoherent {
                 inner: inner1,
                 map_daddr: daddr1,
                 is_cache_coherent,
+                prepared,
             },
             Self {
                 inner: inner2,
                 map_daddr: daddr2,
                 is_cache_coherent,
+                prepared,
             },
         )
     }
@@ -138,8 +374,12 @@ impl Split for DmaCoherent {

 impl Drop for DmaCoherent {
     fn drop(&mut self) {
+        if !self.prepared {
+            return;
+        }
         // SAFETY: The physical address range was prepared in `alloc`.
         unsafe { unprepare_dma(&self.paddr_range(), self.map_daddr) };
+        self.prepared = false;
     }
 }

diff --git a/src/mm/dma/mod.rs b/src/mm/dma/mod.rs
index 84f25bf..2dfc728 100644
--- a/src/mm/dma/mod.rs
+++ b/src/mm/dma/mod.rs
@@ -24,5 +24,5 @@ mod dma_coherent;
 mod dma_stream;
 mod util;

-pub use dma_coherent::DmaCoherent;
+pub use dma_coherent::{DmaCoherent, DmaUnmapFailure, PendingDmaUnmap, UnmappedDma};
 pub use dma_stream::{DmaDirection, DmaStream, FromAndToDevice, FromDevice, ToDevice};
diff --git a/src/mm/dma/util.rs b/src/mm/dma/util.rs
index 6289955..3729e60 100644
--- a/src/mm/dma/util.rs
+++ b/src/mm/dma/util.rs
@@ -173,7 +173,103 @@ pub(super) unsafe fn prepare_dma(pa_range: &Range<Paddr>) -> Option<Daddr> {
 /// The provided physical address range must be untyped DMA memory that was
 /// previously prepared by an [`alloc_unprotect_physical_range()`] call.
 pub(super) unsafe fn unprepare_dma(pa_range: &Range<Paddr>, daddr: Option<Daddr>) {
-    unmap_dma_remap(daddr.map(|daddr| daddr..daddr + pa_range.len()));
+    let daddr_range = daddr.map(|daddr| daddr..daddr + pa_range.len());
+    if let Some(first_daddr) = daddr {
+        // Preserve the legacy arbitrary-size Drop contract without weakening
+        // the ownership API: each bounded begin removes exactly one PTE, then
+        // Drop waits outside every IOMMU lock before trying the next page.
+        for page_offset in (0..pa_range.len()).step_by(PAGE_SIZE) {
+            loop {
+                match begin_dma_unmap(Some(first_daddr + page_offset)) {
+                    BeginDmaUnmap::Direct => unreachable!(),
+                    BeginDmaUnmap::Busy => {
+                        core::hint::spin_loop();
+                        continue;
+                    }
+                    BeginDmaUnmap::Invalidating(pending) => loop {
+                        match poll_dma_unmap(pending) {
+                            PollDmaUnmap::Pending => core::hint::spin_loop(),
+                            PollDmaUnmap::Complete => break,
+                            PollDmaUnmap::RetainedFailure => loop {
+                                // Fail closed: uncertainty must never fall
+                                // through to IOVA/PADDR/backing release.
+                                core::hint::spin_loop();
+                            },
+                        }
+                    },
+                    BeginDmaUnmap::RetainedFailure => loop {
+                        core::hint::spin_loop();
+                    },
+                }
+                break;
+            }
+        }
+    }
+
+    // SAFETY: direct mappings need no IOTLB acknowledgement; remapped ranges
+    // reach this point only after `poll_dma_unmap` observed completion.
+    unsafe { complete_unprepare_dma(pa_range, daddr_range) };
+}
+
+#[derive(Clone, Copy, Debug)]
+pub(super) struct PendingDmaUnmap {
+    ticket: iommu::IotlbInvalidationTicket,
+}
+
+#[derive(Clone, Copy, Debug)]
+pub(super) enum BeginDmaUnmap {
+    Direct,
+    Busy,
+    Invalidating(PendingDmaUnmap),
+    RetainedFailure,
+}
+
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub(super) enum PollDmaUnmap {
+    Pending,
+    Complete,
+    RetainedFailure,
+}
+
+/// Removes the PTEs for one remapped DMA owner and starts one global IOTLB
+/// invalidation.  No physical or device address ownership is released here.
+pub(super) fn begin_dma_unmap(daddr: Option<Daddr>) -> BeginDmaUnmap {
+    let Some(daddr) = daddr else {
+        return BeginDmaUnmap::Direct;
+    };
+
+    match iommu::begin_unmap_invalidate(daddr) {
+        Ok(ticket) => BeginDmaUnmap::Invalidating(PendingDmaUnmap { ticket }),
+        Err(iommu::BeginIotlbInvalidationError::Busy) => BeginDmaUnmap::Busy,
+        Err(iommu::BeginIotlbInvalidationError::PteRemovalFailed) => BeginDmaUnmap::RetainedFailure,
+    }
+}
+
+pub(super) fn poll_dma_unmap(pending: PendingDmaUnmap) -> PollDmaUnmap {
+    match iommu::poll_unmap_invalidate(pending.ticket) {
+        iommu::PollIotlbInvalidation::Pending => PollDmaUnmap::Pending,
+        iommu::PollIotlbInvalidation::Complete => PollDmaUnmap::Complete,
+        iommu::PollIotlbInvalidation::RetainedFailure => PollDmaUnmap::RetainedFailure,
+    }
+}
+
+/// Releases IOVA and PADDR ownership after direct-device quiescence or a
+/// completed IOTLB invalidation.
+///
+/// # Safety
+///
+/// For a remapped range, the corresponding invalidation ticket must have
+/// completed.  For a direct mapping, the caller must have stopped the device.
+pub(super) unsafe fn complete_unprepare_dma(
+    pa_range: &Range<Paddr>,
+    daddr_range: Option<Range<Daddr>>,
+) {
+    if let Some(daddr_range) = daddr_range {
+        let irq_guard = irq::disable_local();
+        #[cfg(target_arch = "x86_64")]
+        allocator::daddr_allocator(&irq_guard).free(daddr_range);
+        drop(irq_guard);
+    }

     // SAFETY: The safety condition is upheld by the caller.
     #[cfg(any(debug_assertions, all(target_arch = "x86_64", feature = "cvm_guest")))]
@@ -284,20 +380,3 @@ unsafe fn dma_remap(pa_range: &Range<Paddr>) -> Option<Daddr> {

     Some(daddr.start)
 }
-
-fn unmap_dma_remap(daddr_range: Option<Range<Daddr>>) {
-    let Some(da_range) = daddr_range else {
-        return;
-    };
-
-    let _irq_gurad = irq::disable_local();
-
-    // TODO: Free `da_range` to `allocator::daddr_allocator()`. This can only
-    // be done after IOTLB flushes are supported; otherwise, reusing the freed
-    // device address range may cause data corruption.
-
-    for da in da_range.step_by(PAGE_SIZE) {
-        iommu::unmap(da).unwrap();
-        // FIXME: Flush IOTLBs to prevent any future DMA access to the frames.
-    }
-}
diff --git a/src/arch/x86/cpu/context/mod.rs b/src/arch/x86/cpu/context/mod.rs
--- a/src/arch/x86/cpu/context/mod.rs
+++ b/src/arch/x86/cpu/context/mod.rs
@@ -354,0 +355 @@ impl UserContextApiInternal for UserContext {
+                    crate::arch::timer::complete_irq(self.as_trap_frame().trap_num as u8);
diff --git a/src/arch/x86/timer/apic.rs b/src/arch/x86/timer/apic.rs
--- a/src/arch/x86/timer/apic.rs
+++ b/src/arch/x86/timer/apic.rs
@@ -18 +18 @@ use crate::{
-/// Initializes APIC with TSC-deadline mode or periodic mode.
+/// Initializes APIC with TSC-deadline mode or callback-rearmed one-shot mode.
@@ -25 +25 @@ pub(super) fn init_on_bsp() -> IrqLine {
-        init_periodic_mode_config();
+        init_callback_rearmed_oneshot_config();
@@ -40,2 +40,2 @@ pub(super) fn init_on_ap(timer_irq: &IrqLine) {
-/// A callback that needs to be called on timer interrupt.
-pub(super) fn timer_callback() {
+/// Rearms the local timer after the complete timer IRQ path has returned.
+pub(super) fn rearm_timer() {
@@ -50 +50,5 @@ pub(super) fn timer_callback() {
-        Config::PeriodicMode { .. } => {}
+        Config::CallbackRearmedOneShot { init_count } => {
+            let preempt_guard = disable_preempt();
+            let apic = apic::get_or_init(&preempt_guard as _);
+            apic.set_timer_init_count(*init_count);
+        }
@@ -69,2 +73,2 @@ fn init_timer(timer_irq: &IrqLine) {
-        Config::PeriodicMode { init_count } => {
-            init_periodic_mode(apic, timer_irq, *init_count);
+        Config::CallbackRearmedOneShot { init_count } => {
+            init_callback_rearmed_oneshot(apic, timer_irq, *init_count);
@@ -79 +83 @@ fn init_deadline_mode(apic: &dyn Apic, timer_irq: &IrqLine) {
-    timer_callback();
+    rearm_timer();
@@ -82,4 +86 @@ fn init_deadline_mode(apic: &dyn Apic, timer_irq: &IrqLine) {
-fn init_periodic_mode(apic: &dyn Apic, timer_irq: &IrqLine, init_count: u64) {
-    // Enable periodic mode
-    apic.set_timer_init_count(init_count);
-    apic.set_lvt_timer(timer_irq.num() as u64 | (1 << 17));
+fn init_callback_rearmed_oneshot(apic: &dyn Apic, timer_irq: &IrqLine, init_count: u64) {
@@ -86,0 +88,3 @@ fn init_periodic_mode(apic: &dyn Apic, timer_irq: &IrqLine, init_count: u64) {
+    apic.set_lvt_timer(timer_irq.num() as u64);
+    // Arm last so the full interval starts after mode and divisor are installed.
+    apic.set_timer_init_count(init_count);
@@ -93 +97 @@ enum Config {
-    PeriodicMode { init_count: u64 },
+    CallbackRearmedOneShot { init_count: u64 },
@@ -103,2 +107,2 @@ fn init_deadline_mode_config() {
-fn init_periodic_mode_config() {
-    info!("Enable APIC periodic mode");
+fn init_callback_rearmed_oneshot_config() {
+    info!("Enable APIC callback-rearmed one-shot mode");
@@ -166 +170 @@ fn init_periodic_mode_config() {
-        CONFIG.call_once(|| Config::PeriodicMode {
+        CONFIG.call_once(|| Config::CallbackRearmedOneShot {
diff --git a/src/arch/x86/timer/mod.rs b/src/arch/x86/timer/mod.rs
--- a/src/arch/x86/timer/mod.rs
+++ b/src/arch/x86/timer/mod.rs
@@ -41,0 +42 @@ fn timer_callback(trapframe: &TrapFrame) {
+}
@@ -43 +44,7 @@ fn timer_callback(trapframe: &TrapFrame) {
-    apic::timer_callback();
+pub(super) fn complete_irq(irq_num: u8) {
+    let Some(timer_irq) = TIMER_IRQ.get() else {
+        return;
+    };
+    if timer_irq.num() == irq_num {
+        apic::rearm_timer();
+    }
diff --git a/src/arch/x86/trap/mod.rs b/src/arch/x86/trap/mod.rs
--- a/src/arch/x86/trap/mod.rs
+++ b/src/arch/x86/trap/mod.rs
@@ -192,0 +193 @@ unsafe extern "sysv64" fn trap_handler(f: &mut TrapFrame) {
+            super::timer::complete_irq(f.trap_num as u8);
diff --git a/src/task/mod.rs b/src/task/mod.rs
--- a/src/task/mod.rs
+++ b/src/task/mod.rs
@@ -39,0 +40,4 @@ static POST_SCHEDULE_HANDLER: Once<fn()> = Once::new();
+static FIRST_TASK_PRE_IRQ_HANDLER: Once<fn(&DisabledLocalIrqGuard)> = Once::new();
+
+static FIRST_TASK_ENTRY_HANDLER: Once<fn()> = Once::new();
+
@@ -51,0 +66,10 @@ pub fn inject_post_schedule_handler(handler: fn()) {
+/// Injects a first-switch handler that runs before local IRQs are enabled.
+pub fn inject_first_task_pre_irq_handler(handler: fn(&DisabledLocalIrqGuard)) {
+    FIRST_TASK_PRE_IRQ_HANDLER.call_once(|| handler);
+}
+
+/// Injects a handler after OSTD finishes a new task's switch tail.
+pub fn inject_first_task_entry_handler(handler: fn()) {
+    FIRST_TASK_ENTRY_HANDLER.call_once(|| handler);
+}
+
@@ -198 +215 @@ impl TaskOptions {
-            unsafe { processor::after_switching_to() };
+            unsafe { processor::after_switching_to(processor::SwitchTailKind::FirstTask) };
@@ -198,0 +216,4 @@ impl TaskOptions {
+
+            if let Some(handler) = FIRST_TASK_ENTRY_HANDLER.get() {
+                handler();
+            }
diff --git a/src/task/processor.rs b/src/task/processor.rs
--- a/src/task/processor.rs
+++ b/src/task/processor.rs
@@ -21,0 +22,6 @@ cpu_local_cell! {
+#[derive(Clone, Copy, Debug, Eq, PartialEq)]
+pub(super) enum SwitchTailKind {
+    FirstTask,
+    ResumedTask,
+}
+
@@ -92 +92 @@ pub(super) fn switch_to_task(next_task: Arc<Task>) {
-    unsafe { after_switching_to() };
+    unsafe { after_switching_to(SwitchTailKind::ResumedTask) };
@@ -119 +119 @@ pub(super) unsafe fn after_switching_to() {
-pub(super) unsafe fn after_switching_to() {
+pub(super) unsafe fn after_switching_to(tail_kind: SwitchTailKind) {
@@ -140,0 +141,15 @@ pub(super) unsafe fn after_switching_to() {
+    if tail_kind == SwitchTailKind::FirstTask {
+        assert!(
+            !crate::arch::irq::is_local_enabled(),
+            "first-task admission must run with local IRQs disabled"
+        );
+        let admission_guard = crate::irq::disable_local();
+        if let Some(handler) = super::FIRST_TASK_PRE_IRQ_HANDLER.get() {
+            handler(&admission_guard);
+        }
+        assert!(
+            !crate::arch::irq::is_local_enabled(),
+            "first-task admission handler must preserve disabled local IRQs"
+        );
+        core::mem::forget(admission_guard);
+    }
diff --git a/src/timer/jiffies.rs b/src/timer/jiffies.rs
--- a/src/timer/jiffies.rs
+++ b/src/timer/jiffies.rs
@@ -12,2 +12,2 @@ use super::TIMER_FREQ;
-/// A jiffy represents one tick of the system timer interrupt,
-/// whose frequency is equal to [`TIMER_FREQ`] Hz.
+/// A jiffy represents one observed system timer callback. [`TIMER_FREQ`] is
+/// the nominal conversion rate, not a wall-clock progress guarantee.
@@ -28 +28 @@ impl Jiffies {
-    /// Returns the elapsed time since the system boots up.
+    /// Returns the number of observed timer callbacks since boot.
@@ -43 +43 @@ impl Jiffies {
-    /// Gets the [`Duration`] calculated from the jiffies counts.
+    /// Gets the nominal [`Duration`] calculated from the jiffies count.
diff --git a/src/timer/mod.rs b/src/timer/mod.rs
--- a/src/timer/mod.rs
+++ b/src/timer/mod.rs
@@ -18 +18 @@ use crate::{
-/// The timer frequency in Hz.
+/// The nominal maximum timer callback frequency in Hz.
@@ -24 +24,2 @@ use crate::{
-/// spent in executing timer code.
+/// spent in executing timer code. An architecture may rearm only after a callback completes, so
+/// callback handling can make the effective wall-clock frequency lower than this value.
